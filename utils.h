#ifndef TP_HASH_UTILS_H
#define TP_HASH_UTILS_H
#include "stddef.h"
#include "stdbool.h"
size_t siguiente_primo(size_t numero);
bool es_primo(size_t primo);
double sqrt(double numero);
#endif //TP_HASH_UTILS_H
